# Execution Layer

Deterministic implementation code is located in:

- `src/tg_time_logger/agents/execution/`
- `src/tg_time_logger/agents/tools/`

This keeps runtime imports stable while preserving the `agents/` architecture map in the repository root.
