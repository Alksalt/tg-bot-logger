# Agents Directory Map

- `directives/` human-readable SOP/spec files.
- `execution/` pointer to deterministic runtime implementation.
- `orchestration/` pointer to thin orchestration implementation.
- `tools/` pointer to tool implementation and tool manifest.
- `models.yaml` model-tier routing policy.

Runtime code lives under `src/tg_time_logger/agents/` for import stability.
